Retrieval ranking systems often separate candidate generation from final ordering. A query is first matched against document representations using similarity measures such as cosine similarity over embeddings. A ranking policy may then reorder results according to additional constraints, including trust, source quality, or domain-specific governance requirements. This separation makes it possible to inspect how semantic relevance and policy intervention interact instead of collapsing both into a single opaque score.