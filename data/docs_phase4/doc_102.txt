Search and retrieval systems usually find documents by comparing a user query with stored document representations. After that first match step, results can be sorted again using other rules. Some systems consider source quality or document importance in addition to similarity. This means that the document that sounds most similar to the query is not always the one shown first, because ranking can include other priorities beyond semantic closeness.