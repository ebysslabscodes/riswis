Trust weighting in retrieval is a policy mechanism that adjusts final ranking after semantic similarity has been computed. A system may assign multipliers to different source classes so that highly trusted material receives greater influence in final ordering. This does not change the underlying semantic match itself. Instead, it changes how the system governs which relevant results are elevated, preserved, or suppressed when semantic strength and source trust do not align.